#include <bits/stdc++.h>
using namespace std;

fibonacci(){
    return fibonacci()+
}
int main() {
	// your code goes here
    int n,q;cin >> n >> q;
    vector<int> nums(n);
    vector<int> dp(n+1);
    for(int i = 0; i  < n ; i++){
        cin >> nums[i];
    }
    fibonacci(0 )
}
