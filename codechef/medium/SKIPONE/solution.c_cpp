#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
    int t; cin >> t;
    while(t--){
        int n,k; cin >> n >> k;
        vector<int> nums(n);
        int sum = 0;
        bool discount = true;
        int ans = 1;
        int maxi = 0;
        for(int i = 0; i < n; i++) cin >> nums[i];
        
        cout << ans << '\n';
    }
}
