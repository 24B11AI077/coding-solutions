#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
    string s,p; cin >> s >> p;
    int k = p.size();
    unordered_map<int,int> mp;
    for(int i = 0; i < k ; i++){
        mp[s[i]]++;
    }
    for(int i = k; i < s.size(); i++){
        
    }
}
