#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
    int w; cin >> w;
    int n ;cin >> n;
    vector<int> nums(n);
    for(int i = 0; i < n ; i++){
        cin >> nums[i];
    }
    sort(nums.rbegin(),nums.rend());
    int obt = 0;
    for(int i = 0; i < n ; i++){
        if(nums[i] >= )
    }
}
